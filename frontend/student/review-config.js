
const REVIEW_CONFIG = {
    // Keep this true until the backend API is ready.
    USE_MOCK_DATA: false,

    // The backend developer will provide the correct endpoint later.
    API_BASE_URL: "http://localhost:8000",

    REVIEW_ENDPOINT: "/attempts/{attemptId}/review"
};